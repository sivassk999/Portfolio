// Initialize the Typed instance with the correct constructor and property names
var typed = new Typed(".text", {
    strings: ["Frontend Developer", "Web Developer"], // 'strings' should be in lowercase
    typeSpeed: 100,
    backSpeed: 100,
    backDelay: 1000,
    loop: true
});
